# AGENTS.md

本仓库包含 oh-my-posh 主题配置。修改主题时请遵守以下规则。

## 双版本主题必须同步修改（重要）

部分主题同时存在两个版本，例如：

- `mocha-capsule.omp.json` — 完整版（依赖 Nerd Font 字体）
- `mocha-capsule-nofont.omp.json` — 无字体版（不依赖 Nerd Font）

**规则：当你修改任一版本的主题时，必须同时修改它的另一个版本，不能只改其一。**

### 同步要求

1. **效果同步**：两个版本应呈现尽可能一致的视觉效果（布局、分段、配色、分隔符风格等）。无字体版中不依赖 Nerd Font 的部分（如 `text` 图标）应使用与完整版相同的字符；无法复用的仅限需要 Nerd Font 字形的图标（如 `\uf0a9` 之类私有区字符）。
2. **实现同步**：结构性改动（增删 segment、调整 `type`、`style`、`powerline_symbol`、`properties` 等逻辑）应在两个版本中保持一致，仅在图标呈现方式上允许差异。
3. **修改流程**：
   - 先确认主题是否以 `xxx` / `xxx-nofont` 成对存在（检查 `themes/` 目录）。
   - 若成对存在，对修改的每处变更，都检查另一个版本是否也需要对应修改。
   - 提交前逐一对比两个版本，确认除字体图标外逻辑一致。

### 判定标准

- 完整版中的 Nerd Font 图标在无字体版中替换为等价的普通字符/emoji，并保持语义相同。
- 颜色值、透明度、模板表达式、条件判断在两个版本中必须完全相同。
- 新增主题时，如内容依赖 Nerd Font，应同时提供 `-nofont` 版本。

## 长模板必须拆分（重要）

单个 `template` 字符串不应包含多个条件逻辑拼在一起的长行（如 git、time 分段那种数百字符的模板）。超过大约 80–100 字符或包含 2 个以上条件块的模板，必须拆分为 `templates` 数组。

### 拆分方式

- 使用 `templates` 数组替代 `template`（不写 `templates_logic`，默认 `join`：按数组顺序拼接每个子模板的渲染结果，渲染为纯空白的子模板会被跳过）。
- 每个数组元素只承载一段逻辑：固定文本 + 一个条件块。例如 git 段拆为：HEAD/分支 → working → `|` 分隔符 → staging → stash → ahead/behind → 收尾字符。
- `if/else if` 链必须保留在同一元素内，不能拆到多个元素（各元素独立渲染，跨元素会导致条件重复命中）。
- 元素开头/结尾有意义的空格必须保留在元素内，不能只留在数组边界外。

### 两个必须注意的坑

1. **末尾空格不能单独成元素**：整个模板结尾的空格若作为一个独立元素，会因是纯空白被 join 跳过；也不能把它移入 `trailing_diamond`——diamond 里的空格渲染在分段背景色之外，胶囊与菱形之间会出现一个无背景色的"空洞"（视觉上像多了一个空格）。正确做法：把 `</> `（markup 重置 + 空格）追加到**最后一个数组元素**的末尾。`</> ` 的原始值含非空白字符，join 不会跳过它；markup 处理后 `</>` 恢复分段自身颜色（跟随 `background_templates`），空格便落在背景色内，与拆分前渲染一致。
2. **括号/闭合标记**：无字体版模板尾部形如 `)</>` 的闭合内容可单独成元素（非空白不会被跳过）。

### 同步与验证

- 两个版本的拆分位置与元素结构必须一致，仅图标字形和首尾闭合字符允许按版本差异。
- 拆分后必须验证渲染结果与拆分前逐字节一致：用 `oh-my-posh print primary --config <配置> --data <数据文件> --data-only --plain` 覆盖各 git 状态（clean / dirty / staged / ahead / behind）分别对比修改前后的输出（用 `cmd /c "git show HEAD:<文件> > <临时文件>"` 字节级导出旧配置，避免 PowerShell 管道按系统代码页误解码）。
